version: "3.8"

services:
  app:
    build: .
    volumes:
      - .:/app
    expose:
      - "9000"

  nginx:
    image: nginx:latest
    ports:
      - "8001:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - app
