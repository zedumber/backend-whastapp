version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8001:8000"
    volumes:
      - .:/app
    environment:
      APP_ENV: production
      APP_DEBUG: false
      APP_KEY: base64:YourAppKeyHere
